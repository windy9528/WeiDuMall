<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="bwie.com.weidumall">

    <uses-permission android:name="android.permission.ACCESS_NETWORK_STATE" />
    <uses-permission android:name="android.permission.INTERNET" />
    <uses-permission android:name="android.permission.READ_EXTERNAL_STORAGE"/>
    <uses-permission android:name="android.permission.WRITE_EXTERNAL_STORAGE"/>
    <uses-permission android:name="android.permission.CAMERA"/>

    <application
        android:name=".common.App"
        android:allowBackup="true"
        android:icon="@mipmap/app_icon"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/app_icon"
        android:supportsRtl="true"
        android:theme="@style/AppTheme">
        <activity
            android:name=".view.MainActivity"
            android:label="@string/app_name"></activity>
        <activity android:name=".view.activity.user.WelcomeActivity">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />
                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
        <activity android:name=".view.activity.user.LoginActivity" />
        <activity android:name=".view.activity.user.RegisterActivity" />
        <activity android:name=".view.activity.home.DetailActivity" />
        <activity android:name=".view.activity.home.SearchActivity" />
        <activity android:name=".view.activity.circle.PublishActivity" />
        <activity android:name=".view.activity.user.PersonZActivity" />
        <activity android:name=".view.activity.user.PersonDActivity" />
    </application>

</manifest>